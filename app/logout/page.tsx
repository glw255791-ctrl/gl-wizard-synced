// eslint-disable-next-line react-refresh/only-export-components
export const metadata = {
  title: "Logout - GL Wizard",
};

import { LogoutPage } from "@/components/pages/auth/logout/logout";

export default function Page() {
  return <LogoutPage />;
}
