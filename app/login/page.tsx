"use client";

// eslint-disable-next-line react-refresh/only-export-components
export const metadata = {
  title: "Login - GL Wizard",
};

import { LoginPage } from "@/components/pages/auth/login/login";

export default function Page() {
  return <LoginPage />;
}
